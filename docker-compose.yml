services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: protasker_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: protasker
      MYSQL_USER: protasker_user
      MYSQL_PASSWORD: protasker_password
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql/init:/docker-entrypoint-initdb.d
    networks:
      - protasker_network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # Laravel Backend API
  backend:
    build:
      context: ./protasker-api
      dockerfile: Dockerfile
    container_name: protasker_backend
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./protasker-api:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    ports:
      - "8000:8000"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - protasker_network
    environment:
      - DB_CONNECTION=mysql
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_DATABASE=protasker
      - DB_USERNAME=protasker_user
      - DB_PASSWORD=protasker_password
    command: >
      sh -c "
        composer install &&
        php artisan key:generate &&
        php artisan migrate:fresh --seed &&
        php artisan serve --host=0.0.0.0 --port=8000
      "

  # Vue.js Frontend
  frontend:
    build:
      context: ./protasker-web
      dockerfile: Dockerfile
    container_name: protasker_frontend
    restart: unless-stopped
    volumes:
      - ./protasker-web:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - protasker_network
    environment:
      - VITE_API_URL=http://localhost:8000/api
    command: >
      sh -c "
        npm install &&
        npm run dev -- --host 0.0.0.0
      "

  # Nginx (Optional - for production)
  nginx:
    image: nginx:alpine
    container_name: protasker_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
      - ./protasker-web/dist:/var/www/frontend
      - ./protasker-api/public:/var/www/backend
    depends_on:
      - backend
      - frontend
    networks:
      - protasker_network
    profiles:
      - production

volumes:
  mysql_data:
    driver: local

networks:
  protasker_network:
    driver: bridge